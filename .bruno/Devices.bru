meta {
  name: Devices
  type: http
  seq: 5
}

get {
  url: {{host}}/api/devices
  body: none
  auth: inherit
}

vars:post-response {
  deviceId: res.body[0].deviceId
}

settings {
  encodeUrl: true
  timeout: 0
}
