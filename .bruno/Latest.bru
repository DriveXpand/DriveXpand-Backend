meta {
  name: Latest
  type: http
  seq: 4
}

get {
  url: {{host}}/api/telemetry/latest?deviceId={{deviceId}}
  body: none
  auth: inherit
}

params:query {
  deviceId: {{deviceId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
