meta {
  name: trips without details
  type: http
  seq: 7
}

get {
  url: {{host}}/api/trips/list?deviceId={{deviceId}}
  body: none
  auth: inherit
}

params:query {
  deviceId: {{deviceId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
