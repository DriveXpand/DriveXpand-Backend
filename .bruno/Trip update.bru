meta {
  name: Trip update
  type: http
  seq: 12
}

patch {
  url: {{host}}/api/trips/{{tripId}}
  body: json
  auth: inherit
}

body:json {
  {
    "startLocation": "F-Town",
    "endLocation": "Zuhause"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
